By ID

WebElement obj1 = driver.findElement(By.id("Available id"));

By NAME

WebElement obj2 = driver.findelement(By.name("availbale name"));

By LINK

webElement obj3= driver.findelement(By.linktext("text which is available inbetween anchor tag"));

By PARTIAL LINK

webElement obj4 = driver.findElement(By.partiallinktext("partial text which is available in between anchor tag"))

When there is select tag available then

Select select = new select(driver.findElement(By.tagname("select")));

select.selectByValue("15");

select.selectByVisibleText("yes it is");

By CLASS NAME

WebElement obj5 = driver.findelement(by.classname("class name"));



X path 

WebElement obj6 = driver.findelements(by.xpath(//tagname [@name='e mail'])

with id : //classname[@id="email"] or //*[@id="e mail"]

with name : //classname[@name="e mail"] or //*[@name="e mail"]

with link text : //classname[.='log in']

                  //a[@href='url']

With starts with : //tagname[starts-with(@id,'email')]

With text : //tagname[text()='userID']

With contains : //tagname[contains(@type,'sub')]

with or : //*[@type='color'  or  @id ='email']

with And : //tagname[@type='submit' AND @name='btnLogin']




CSS selecter

WebElement obj6 = driver.findelements(by.cssselector("input[id="email"]"));



css selector by attribute 

I.E 

<input type="text" id="fistname" name="first_name" class="myForm">

css = element_name[<attribute_name>='<value>']


with id : css=input[name=’first_name’]

           css=input#id
    
           css="#firstname"

with classname : css=input[class='myForm’]
       
                 css="input.myForm"
     
                 css=".myForm"

with more attribute : css="input[type="text'][name='first_name]"




With name : css= input[name=email]

With link: css=a[href='url']

with link text : css=div:contains('Sign')

With text : css=tag:contains(“inner text”)

with class name and attribute value : css=tag.class[attribute=value]



Locating child element 

<ul id="fruit">
    <li>Apple</li>
    <li>Orange</li>
    <li>Banana</li>
</ul>

css="ul#fruit li:nth-of-type(2)"

for selecting last child 

css="ul#fruit li:last-child"


Locating with position of text

<div id="123_randomId">
<div id="randomId_456">
<div id="123_pattern_randomId">


Starts with : css="div[id^='123']"

Ends with : css="div[id$='456']"

Sub string : css="div[id*='_pattern_']"

also contains can be use : css="div:contains(_pattern_)"





<div class="day past calendar-day-2017-02-13 calendar-dow-1 unavailable">
<div class="day today calendar-day-2017-02-14 calendar-dow-2 unavailable">
<div class="day calendar-day-2017-02-15 calendar-dow-3">
<div class="day calendar-day-2017-02-16 calendar-dow-4">

In the above snippet, we want to select an available day (i.e. the two last divs)
As can be seen, all four divs contain “calendar-day-” but the first two also contain “unavailable” which we don’t want.
The CSS selector for Not selecting the first two divs is

css = "div[class*=calendar-day-]:not([class*='unavailable'])"




























